flask>=3.0,<4.0
# TensorFlow is intentionally omitted because no official wheel is available for Python 3.14.
# If you switch to Python 3.11/3.10 or use Docker, add TensorFlow back:
# tensorflow>=2.15,<2.17
# scikit-learn and numpy are intentionally omitted on this platform to avoid building from source
# which often fails on Windows/32-bit or when a C toolchain is unavailable.
# If you need the real models later, install scikit-learn and numpy via conda or use a 64-bit Python:
# conda install -c conda-forge scikit-learn numpy
# Keep only lightweight runtime deps so the app can run on Python 3.14 without building wheels
joblib>=1.3,<1.5
requests>=2.31,<3.0
flask>=3.0,<4.0
